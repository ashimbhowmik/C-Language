#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main()
{

    char a[100];
    // int i, count = 0;

    scanf("%s", &a);
    int sn = strlen(a);

    // for (i = 0; a[i] != '\0'; i++)
    // {

    //     count++;
    // }
    // printf("%d", count);
    printf("%d", sn);

    return 0;
}