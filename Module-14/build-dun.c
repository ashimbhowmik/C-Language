#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{

    // double x;
    // scanf("%lf", &x);

    // // int ans = ceil(x);
    // // int ans = floor(x);
    // // int ans = round(x);

    // printf("%d", ans);

    // root er jonno sqrt......

    // double x;
    // scanf("%lf", &x);

    // double ans = sqrt(x);

    // printf("%0.2lf", ans);

    // power..................

    // double x, y;
    // scanf("%lf %lf", &x, &y);

    // double ans = pow(x, y);

    // printf("%0.2lf", ans);

    // abs() sudhu int a kaj kore..........

    int x;
    scanf("%d", &x);

    int ans = abs(x);

    printf("%d", ans);

    return 0;
}